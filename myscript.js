/* 
* Script that handles interactive elements of input list
* adds and removes input
*/
var x = document.getElementById('inputList').innerHTML;
console.log(x);

// Input object constructor
function input (number, label) {
	this.number = number;
	this.label = label;
}

// Array of inputs
var inputsArray = [];

// Add a new input
function addInput () {
	var 
}
